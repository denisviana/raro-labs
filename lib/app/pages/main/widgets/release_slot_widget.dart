import 'package:flutter/material.dart';

class ReleaseSlotWidget extends StatelessWidget {
  const ReleaseSlotWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
